// $(document).on('keypress','#search-host-input', function (e) {
//     const keycode = (e.keyCode ? e.keyCode : e.which);

//     if (keycode == '13') {
//         e.stopPropagation();

//         // Search host
//         myhost.search();
//     }
// });

// $(document).on('keypress','#search-package-input', function (e) {
//     const keycode = (e.keyCode ? e.keyCode : e.which);

//     if (keycode == '13') {
//         e.stopPropagation();

//         // Search package
//         myhost.searchPackage();
//     }
// });
